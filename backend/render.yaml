services:
  - type: web
    name: seranjivi-crm-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8000
