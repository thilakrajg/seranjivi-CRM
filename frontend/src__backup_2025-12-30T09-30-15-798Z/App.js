export default function App() {
  return (
    <div style={{ padding: 16, fontFamily: "system-ui, Arial, sans-serif" }}>
      <h1 style={{ margin: 0 }}>App started</h1>
      <p style={{ marginTop: 8 }}>
        Your project was missing the Create React App folders <code>public/</code> and
        <code>src/</code>. They were recreated so <code>npm start</code> works again.
      </p>
    </div>
  );
}
